# version="1.0"
# description=”Docker file to
# build the mapping image.”

FROM node:latest
MAINTAINER Dupeydoo

RUN npm install express
RUN npm install request

WORKDIR /usr/src/mapping
CMD node mapping.js
